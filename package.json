{
  "name": "sidethreadgpt",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=iife --outfile=content.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
